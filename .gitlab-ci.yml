stages:
    - test
    - build
    - deploy

variables: 
    MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"
    SONAR_HOST_URL: "http://localsonarqube:9000/"
    SONAR_TOKEN: "squ_35c667f3567119d5702ffa5fb92ca6eaac5889a6"
    SONAR_USER: "gitlab-runner"
    SONAR_PASSWORD: "Password@123"
    DEPLOY_STAGING_SSH_HOST: "ec2-user@172.16.1.121"
default:
    cache:
        paths:
            - .m2/repository/
            - target/
    image: maven:3-openjdk-11

test:
    stage: test
    script:
      - mvn verify sonar:sonar -U
    rules:
      - if: $CI_COMMIT_BRANCH == "staging"
        allow_failure: true
      - if: $CI_COMMIT_BRANCH != "staging"
        allow_failure: false
    artifacts:
        untracked: false
        name: test_and_coverage_report
        reports:
          junit: [ api/target/surefire-reports/*.xml ]
        expire_in: "1 week"

build_java:
    stage: build
    script:
      - mkdir ~/.ssh
      - chmod 700 ~/.ssh 
      - echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config
      - mvn -s /tmp/maven_settings/settings-repo_linkit.xml -U deploy -Pwar -DskipTests=true 
    artifacts:
        paths:
          - api/target/govregistry.war
          - src/main/resources/*.sql
    only:
      - main
      - staging

build_angular:
    stage: build
    image: node:14.18.2-alpine3.15
    script:
      - cd webapp
      - npm install
      - npm run production-govregistry
      - tar czf govregistry-app.tgz dist/*  
    artifacts:
        paths:
          - webapp/govregistry-app.tgz
    only:
      - main
      - staging

deploy_staging:
  stage: deploy
  image: kroniak/ssh-client
  script:
    - cp -r /tmp/ssh/ ~/.ssh
    - scp -r webapp/govregistry-app.tgz $DEPLOY_STAGING_SSH_HOST:/tmp/
    - scp -r api/target/govregistry.war $DEPLOY_STAGING_SSH_HOST:/tmp/
    - ssh $DEPLOY_STAGING_SSH_HOST 'sudo tar xvf /tmp/govregistry-app.tgz'
    - ssh $DEPLOY_STAGING_SSH_HOST 'sudo systemctl stop tomcat'
    - ssh $DEPLOY_STAGING_SSH_HOST 'sudo cp -r dist/govregistry-app/* /usr/share/nginx/html/govregistry-app/'
    - ssh $DEPLOY_STAGING_SSH_HOST 'sudo cp -r /tmp/govregistry.war /opt/apache-tomcat-9.0.70/webapps/'  
    - ssh $DEPLOY_STAGING_SSH_HOST 'sudo systemctl start tomcat'
    - ssh $DEPLOY_STAGING_SSH_HOST 'sudo rm -rf /tmp/govregistry-app.tgz dist /tmp/govregistry.war'
  only:
    - staging
